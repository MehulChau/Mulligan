export default function App() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexDirection: "column",
        gap: "0.5rem",
        background: "#eae6da",
        color: "#1f3d2b",
        fontFamily: "'Archivo', sans-serif",
      }}
    >
      <h1 style={{ fontWeight: 800 }}>Mulligan</h1>
      <p>Scaffold OK — game loop comes in step 4.</p>
    </div>
  );
}
